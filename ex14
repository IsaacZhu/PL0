var i,j;
begin
  // you can use (j != 0) for "j"
	//i:=21;
	//j:=11;
 if i>j && j&& j > 10 && i && (i>20) || i>100 || j >= 100 && i <= 40 && (j <= 20 || i >= -10) {
 		i := 2;
	}
	printf("i is @0\n",i);

end.
